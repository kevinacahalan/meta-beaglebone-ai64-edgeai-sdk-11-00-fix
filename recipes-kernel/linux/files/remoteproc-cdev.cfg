# Required by TI TIOVX/TIDL vision-apps: app_mem opens /dev/remoteproc0 to
# translate dma_buf FDs for the remote cores. Without the remoteproc cdev,
# /dev/remoteproc0 does not exist and EdgeAI demos fail with
# "MEM: ERROR: /dev/remoteproc0 open failed".
CONFIG_REMOTEPROC_CDEV=y
